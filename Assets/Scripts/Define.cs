namespace Enums
{
    public enum PLAYER_STATE
    {
        IDLE,
        RUN,
        JUMP
    }
    public enum COLOR
    {
        RED,
        BLUE
    }

    //test
    public enum GameState
    {
        Ttitle,
        Platformer,
        Dialogue, 
        End,
    }

    public enum MissionState
    {
        Ready,
        InProgress,
        Complete,
        Fail
    }
    public enum TeleScreenType
    {
        Ready,
        On,
        Off
    }
}

